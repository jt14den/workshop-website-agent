# CLAUDE.md — Carpentries Workshop Website Agent

> **Full instructions:** read `instructions/core.md` at the start of every session.
> This file is a compatibility shim for Claude Code. All substantive guidance lives in `instructions/core.md`.

---

## Critical Rules (enforced even if you skip core.md)

1. **Never fork** — use `gh repo create <owner>/<repo> --template carpentries/workshop-template --public --clone`
2. **Work on `gh-pages` branch only** — never commit to `main`
3. **Repo name format:** `YYYY-MM-DD-<slug>` — slug must not end with `-` or `_`
4. **Preserve all `_config.yml` variables** — do not delete unused fields
5. **Never invent details** — use `TBD` for anything not in `workshop-facts.yaml`

---

## Your Task

1. Read `instructions/core.md`
2. Read `workshop-facts.yaml`
3. Confirm the facts look complete, then proceed step by step
4. Use scripts where available; apply judgment for schedule drafting and site customization
